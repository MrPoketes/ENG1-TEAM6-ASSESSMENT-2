package com.hardgforgif.dragonboatracing.tests.examples;

import static org.junit.Assert.assertEquals;

import com.hardgforgif.dragonboatracing.tests.GdxTestRunner;
import org.junit.Ignore;
import org.junit.Test;
import org.junit.runner.RunWith;

//Example unit tests - remove the @Ignore tag to have them run during testing.
//These should probably be removed before we hand in the project, but we'll probably forget.
@RunWith(GdxTestRunner.class)
public class UnitTestExample {

	@Ignore
	@Test
	public void oneEqualsOne() {
		assertEquals(1, 1);
	}

	@Ignore
	@Test
	public void oneEqualsTwo() {
		assertEquals(1, 2);
	}

}
